apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: dev
spec:
  hard:
    pods: '10'
    requests.cpu: '1'
    request.memory: 200M
    limits.cpu: '4'
    limits.memory: 1Gi